import type { LiquidityToolbarFilters } from "../../types/liquidity"

export const defaultLiquidityFilters: LiquidityToolbarFilters = {
  outletType: "All outlet types",
  agentQuery: "",
  stockoutState: "all",
  dateRange: "Today",
}
